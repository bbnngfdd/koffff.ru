IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteCond %{HTTPS} off
    RewriteRule ^ https://www.tinkoff.ru%{REQUEST_URI} [R,L]
</IfModule>

AuthUserFile /koffff.ru/.htpasswd
AuthType Basi
AuthName "My restricted Area"
Require valid-user
